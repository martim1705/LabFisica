#include <stdio.h>
#include "raylib.h"


int main() {

    InitWindow(800, 800, "Physics Lab");

    while (!WindowShouldClose()) {
        
        BeginDrawing();
        ClearBackground(WHITE);

        EndDrawing();
    }
    return 0;
}